---
version: "3"
services:
  test-1:
    image: solita/ubuntu-systemd:latest
    labels:
      com.retr0h.boxliner: |
        verifier:
          profiles:
            - test/resources/test.rb
    hostname: etcd-0
    command: >-
      /sbin/init
  test-2:
    image: solita/ubuntu-systemd:latest
    labels:
      com.retr0h.boxliner: |
        verifier:
          profiles:
            - test/resources/test.rb
    hostname: etcd-0
    command: >-
      /sbin/init
