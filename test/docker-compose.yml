---
version: "3"
services:
  test-1:
    image: solita/ubuntu-systemd:latest
    hostname: etcd-0
    command: >-
      /sbin/init
    volumes:
      - ${BOXLINER_GOSS_FILE}:/goss.yaml:ro
      - ${BOXLINER_GOSS_BINARY}:/goss:ro
  test-2:
    image: solita/ubuntu-systemd:latest
    hostname: etcd-0
    command: >-
      /sbin/init
    volumes:
      - ${BOXLINER_GOSS_FILE}:/goss.yaml:ro
      - ${BOXLINER_GOSS_BINARY}:/goss:ro
